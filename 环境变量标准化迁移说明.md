# 环境变量标准化迁移说明

## 1. 先回答几个关键问题

### 1.1 `Portal 做唯一运行时配置源` 是什么意思

意思是：

1. Azure Function 运行时真正读取的配置，统一来自 Azure Portal 的 `Application settings`
2. 包内不再保留第二套“也会影响运行”的配置文件
3. 本地示例文件仅用于展示和本地调试，不参与线上运行

这样做的好处是：

1. 客户在 Portal 上看到什么，运行时就用什么
2. 不会出现“包里是 A，Portal 里是 B，但实际到底谁生效不清楚”的情况
3. 后续调 schedule、dry run、单任务停用时，都只需要在 Portal 改

### 1.2 为什么删掉 `function_runtime_config.json`

删掉它的原因不是功能没用，而是它会形成**第二配置源**。

之前的风险是：

1. 包里 `function_runtime_config.json` 有默认函数名和默认频率
2. Portal 里 App Settings 也能覆盖频率
3. 客户会分不清自己应该看哪里

现在改成：

1. 函数名直接写在代码里
2. 调度统一读 Portal 环境变量
3. 本地模板只保留 `local.settings.example.json`

这样更符合客户“直接在 Portal 看和改”的诉求。

### 1.3 `INTEGRATION_ACTIVE_ENV` 是什么

它表示**当前这套 Function 运行时默认使用哪个环境的公共域名**。

可选值：

- `uat`
- `prod`

代码会这样取：

1. 如果 `INTEGRATION_ACTIVE_ENV=uat`，就优先用 `INTEGRATION_UAT_BASE_URL`
2. 如果 `INTEGRATION_ACTIVE_ENV=prod`，就优先用 `INTEGRATION_PROD_BASE_URL`

因为你已经确认**同一时间只跑一个环境**，所以这个变量是合理的。

注意：

1. 域名可以按环境切换
2. 认证信息也要按环境切换
3. 所以最终建议不是一个 `INTEGRATION_APP_ID`，而是：
   - `INTEGRATION_UAT_APP_ID`
   - `INTEGRATION_UAT_APP_SECRET`
   - `INTEGRATION_PROD_APP_ID`
   - `INTEGRATION_PROD_APP_SECRET`

### 1.4 可以不重新打包吗

**不能直接不打包就部署。**

原因是这次改了：

1. `function_app.py`
2. `defect_forward_sync.py`
3. `glass_master_forward_sync.py`
4. `prod_output_forward_sync.py`
5. `watermark_store.py`
6. 新增 `runtime_settings.py`
7. 删除 `function_runtime_config.json`

这已经是**代码变化**，不是只改 Portal 环境变量。

所以从部署角度看，还是需要一个**新的部署包**。

但有一个好消息：

**不一定要重打依赖，只需要基于当前最新包重新打一个新 zip 即可。**

也就是：

1. 复用当前最新包里的 `.python_packages`
2. 只替换 Python 源码文件
3. 重新压缩成新的 zip

这样不需要重新 `pip install` Linux 依赖，风险和成本都更低。

## 2. 当前环境变量的分组和作用

以下说明基于当前真实环境变量文件：

`/Users/gaokuang/Desktop/圣戈班/customer_rollout/环境变量`

### 2.1 平台基础变量

这些变量建议**保留**，不属于本次标准化重点：

- `AzureWebJobsStorage`：Function 运行和存储基础连接串
- `FUNCTIONS_EXTENSION_VERSION`：Azure Functions 运行时版本
- `FUNCTIONS_WORKER_RUNTIME`：Python 运行时声明
- `APPINSIGHTS_INSTRUMENTATIONKEY`：Application Insights 监控
- `APPLICATIONINSIGHTS_CONNECTION_STRING`：Application Insights 连接串
- `AzureFunctionsJobHost__functionTimeout`：函数超时时间
- `HTTP_PROXY` / `HTTPS_PROXY` / `NO_PROXY`：代理相关配置
- `SCM_DO_BUILD_DURING_DEPLOYMENT`：部署行为控制
- `WEBSITE_CONTENTAZUREFILECONNECTIONSTRING` / `WEBSITE_CONTENTSHARE`：Azure 平台内容存储

### 2.2 ADX 公共变量

当前变量：

- `ADX_CLUSTER_URL`
- `ADX_DATABASE`
- `ADX_TOKEN_SCOPE`
- `STATE_STORAGE_ACCOUNT_URL`
- `WATERMARK_CONTAINER_NAME`

新标准变量：

- `ADX_CLUSTER_URL`
- `ADX_DATABASE`
- `ADX_TOKEN_SCOPE`
- `STATE_STORAGE_ACCOUNT_URL`
- `WATERMARK_CONTAINER_NAME`

处理建议：

1. `STATE_STORAGE_ACCOUNT_URL`、`WATERMARK_CONTAINER_NAME` 直接保留
2. `ADX_*` 作为公共 ADX 连接变量直接保留
3. 同库场景下不建议再配置任务级 `*_ADX_*`
4. `ADX_CLUSTER_URL`、`ADX_DATABASE`、`ADX_TOKEN_SCOPE` 是公共变量，不是产量单独使用的配置

### 2.3 缺陷任务变量

当前变量：

- `ADX_FORWARD_AUDIT_TABLE`
- `ADX_FORWARD_OVERLAP_MINUTES`
- `ADX_FORWARD_SUCCESS_BUFFER_HOURS`
- `ADX_FORWARD_SUCCESS_MAX_LOOKBACK_HOURS`
- `ADX_FORWARD_SUCCESS_MIN_LOOKBACK_HOURS`
- `ADX_FORWARD_SUCCESS_TABLE`
- `ADX_FORWARD_TARGET`
- `ADX_INITIAL_WATERMARK`
- `ADX_WRITE_FORWARD_AUDIT`
- `FORWARD_DRY_RUN`
- `BAITEDA_BASE_URL`
- `BAITEDA_BATCH_SIZE`
- `BAITEDA_BATCH_SLEEP_SECONDS`
- `BAITEDA_MAX_GROUPS_PER_BATCH`
- `BAITEDA_TIMEOUT`

新标准变量：

- `TRACEABILITY_DEFECT_SCHEDULE`
- `TRACEABILITY_DEFECT_DRY_RUN`
- `TRACEABILITY_DEFECT_OVERLAP_MINUTES`
- `TRACEABILITY_DEFECT_FORWARD_TARGET`
- `TRACEABILITY_DEFECT_SUCCESS_TABLE`
- `TRACEABILITY_DEFECT_AUDIT_TABLE`
- `TRACEABILITY_DEFECT_WRITE_AUDIT`
- `TRACEABILITY_DEFECT_INITIAL_WATERMARK`
- `TRACEABILITY_DEFECT_WATERMARK_BLOB_NAME`
- `TRACEABILITY_DEFECT_API_TIMEOUT`
- `TRACEABILITY_DEFECT_API_BATCH_SIZE`
- `TRACEABILITY_DEFECT_API_BATCH_SLEEP_SECONDS`
- `TRACEABILITY_DEFECT_API_MAX_GROUPS_PER_BATCH`

说明：

1. 缺陷任务以前没有显式 Portal schedule 变量，这次建议补上
2. `FORWARD_DRY_RUN` 现在仍兼容，但建议后面改成 `TRACEABILITY_DEFECT_DRY_RUN`

### 2.4 玻璃主数据任务变量

当前变量：

- `GLASS_MASTER_BASE_URL`
- `GLASS_MASTER_BATCH_SIZE`
- `GLASS_MASTER_FORWARD_AUDIT_TABLE`
- `GLASS_MASTER_FORWARD_DRY_RUN`
- `GLASS_MASTER_FORWARD_OVERLAP_MINUTES`
- `GLASS_MASTER_FORWARD_SCHEDULE`
- `GLASS_MASTER_FORWARD_SUCCESS_BUFFER_HOURS`
- `GLASS_MASTER_FORWARD_SUCCESS_MAX_LOOKBACK_HOURS`
- `GLASS_MASTER_FORWARD_SUCCESS_MIN_LOOKBACK_HOURS`
- `GLASS_MASTER_FORWARD_SUCCESS_TABLE`
- `GLASS_MASTER_FORWARD_TARGET`
- `GLASS_MASTER_INITIAL_WATERMARK`
- `GLASS_MASTER_TIMEOUT`
- `GLASS_MASTER_WATERMARK_BLOB_NAME`
- `GLASS_MASTER_WATERMARK_CONTAINER_NAME`
- `GLASS_MASTER_WRITE_FORWARD_AUDIT`

新标准变量：

- `GLASS_MASTER_SCHEDULE`
- `GLASS_MASTER_DRY_RUN`
- `GLASS_MASTER_OVERLAP_MINUTES`
- `GLASS_MASTER_FORWARD_TARGET`
- `GLASS_MASTER_SUCCESS_TABLE`
- `GLASS_MASTER_AUDIT_TABLE`
- `GLASS_MASTER_WRITE_AUDIT`
- `GLASS_MASTER_INITIAL_WATERMARK`
- `GLASS_MASTER_WATERMARK_CONTAINER_NAME`
- `GLASS_MASTER_WATERMARK_BLOB_NAME`
- `GLASS_MASTER_API_TIMEOUT`
- `GLASS_MASTER_API_BATCH_SIZE`

说明：

1. 这组变量当前命名已经接近目标，但还带 `FORWARD_`
2. 本次代码已支持新旧命名并存

### 2.5 产量推送任务变量

当前变量：

- `PROD_FORWARD_AUDIT_TABLE`
- `PROD_FORWARD_DRY_RUN`
- `PROD_FORWARD_LOOKBACK_HOURS`
- `PROD_FORWARD_OVERLAP_MINUTES`
- `PROD_FORWARD_SCHEDULE`
- `PROD_FORWARD_SUCCESS_BUFFER_HOURS`
- `PROD_FORWARD_SUCCESS_MAX_LOOKBACK_HOURS`
- `PROD_FORWARD_SUCCESS_MIN_LOOKBACK_HOURS`
- `PROD_FORWARD_SUCCESS_TABLE`
- `PROD_FORWARD_TARGET`
- `PROD_INITIAL_WATERMARK`
- `PROD_WATERMARK_BLOB_NAME`
- `PROD_WATERMARK_CONTAINER_NAME`
- `PROD_WRITE_FORWARD_AUDIT`

新标准变量：

- `PRODUCTION_OUTPUT_SCHEDULE`
- `PRODUCTION_OUTPUT_DRY_RUN`
- `PRODUCTION_OUTPUT_LOOKBACK_HOURS`
- `PRODUCTION_OUTPUT_OVERLAP_MINUTES`
- `PRODUCTION_OUTPUT_FORWARD_TARGET`
- `PRODUCTION_OUTPUT_SUCCESS_TABLE`
- `PRODUCTION_OUTPUT_AUDIT_TABLE`
- `PRODUCTION_OUTPUT_WRITE_AUDIT`
- `PRODUCTION_OUTPUT_INITIAL_WATERMARK`
- `PRODUCTION_OUTPUT_WATERMARK_CONTAINER_NAME`
- `PRODUCTION_OUTPUT_WATERMARK_BLOB_NAME`
- `PRODUCTION_OUTPUT_API_TIMEOUT`
- `PRODUCTION_OUTPUT_API_BATCH_SIZE`

### 2.6 产量汇总任务变量

当前变量：

- `PROD_ROLLUP_SCHEDULE`

新标准变量：

- `PRODUCTION_ROLLUP_SCHEDULE`

### 2.7 公共接口认证和域名变量

当前变量：

- `BAITEDA_BASE_URL`
- `GLASS_MASTER_BASE_URL`
- `PROD_TELLUS_GET_NEW_TIME_URL`
- `PROD_TELLUS_SYNC_DATA_URL`
- `PROD_TELLUS_APP_ID`
- `PROD_TELLUS_APP_SECRET`
- `PROD_TELLUS_TENANT_ID`
- `PROD_TELLUS_TIMEOUT`
- `PROD_TELLUS_BATCH_SIZE`

新标准变量：

- `INTEGRATION_ACTIVE_ENV`
- `INTEGRATION_UAT_BASE_URL`
- `INTEGRATION_PROD_BASE_URL`
- `INTEGRATION_GET_NEW_TIME_PATH`
- `INTEGRATION_UAT_APP_ID`
- `INTEGRATION_UAT_APP_SECRET`
- `INTEGRATION_PROD_APP_ID`
- `INTEGRATION_PROD_APP_SECRET`
- `INTEGRATION_TENANT_ID`

说明：

1. 现在代码已经支持“按 `INTEGRATION_ACTIVE_ENV` 选择对应环境认证，再由旧变量兜底”
2. 如果后面 Portal 改完，旧 `BAITEDA_* / PROD_TELLUS_*` 可以再考虑删

## 3. 部署新包后，要不要手动改 Portal 环境变量

分两种情况。

### 3.1 如果只想让新包先跑起来

**不一定要立刻手动改 Portal。**

因为当前代码保留了兼容：

1. 旧变量还可以继续被读取
2. 新变量是优先项
3. 老环境暂时不动也能跑

这时你们只需要：

1. 部署新包
2. 如果要用新函数名的单任务停用，再补 `AzureWebJobs.function_xxx.Disabled`

### 3.2 如果想让客户看到一版干净标准的 Portal 配置

那就建议**手动更新 Portal 环境变量**，或者使用我准备的标准化配置文件作为改造基线。

## 4. 是否一定要做兼容

我的建议是：

### 当前阶段：保留兼容

原因：

1. 你们真实环境变量还是旧命名为主
2. 直接硬切会要求 Portal 一次性全量改名
3. 上线风险会更高

### 下一阶段：确认新变量都已切完后，再删兼容

届时可以统一删除这些历史读取：

- `ADX_*`
- `PROD_*`
- `BAITEDA_*`
- `FORWARD_DRY_RUN`
- `GLASS_MASTER_FORWARD_*`

## 5. 配套文件

本次我已经补了两份配套文件：

1. `APP_SETTINGS_REFERENCE.md`
2. `portal_app_settings_标准版.json`

其中：

- `APP_SETTINGS_REFERENCE.md` 用于解释标准变量分组
- `portal_app_settings_标准版.json` 用于给你一版可直接复制参考的配置值
