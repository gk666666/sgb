# 化学品追溯执行流程

## 1. 当前发送环境判断

当前这版化学品追溯代码本身**没有把环境硬编码死在脚本里**，而是按下面顺序取接口地址：

1. `CHEMICAL_TRACEABILITY_API_BASE_URL`
2. `GLASS_MASTER_API_BASE_URL`
3. `GLASS_MASTER_BASE_URL`
4. `INTEGRATION_BASE_URL`
5. 若以上都没配，则继续走 `get_integration_base_url("GLASS_MASTER_BASE_URL", "BAITEDA_BASE_URL")`

接口 URL 也按下面顺序取：

1. `CHEMICAL_TRACEABILITY_API_SYNC_DATA_URL`
2. `CHEMICAL_TRACEABILITY_SYNC_DATA_URL`
3. `GLASS_MASTER_API_SYNC_DATA_URL`
4. `GLASS_MASTER_SYNC_DATA_URL`
5. 若以上都没配，则自动拼接 `/sgb-fm-extend/api/pub/sync/plc/glass/defect/sync`

所以：

- **按当前交付包的模板和 Portal 模板，默认是发 UAT**
- 因为模板里的基准域名是：`https://sekurit.tellus-uatt.saint-gobain.com.cn`
- 但如果线上 Function App 的这些环境变量被改成了生产地址，实际发送环境也会跟着切走

结论：

- **当前我交付的这套默认配置，目标环境是 UAT**
- **最终仍以 Portal 中实际生效的环境变量为准**

## 2. 链路总览

本次化学品追溯链路不是 `Event Hub -> Function 直接触发推接口`，而是下面这条正式链路：

`EMQX -> Event Hub -> ADX raw -> ADX std -> ADX forward candidates -> Azure Function Timer -> 客户接口`

这样做的原因是：

1. 原始报文先沉淀到 ADX
2. 生成固定 4 字段后落正式表 `chemical_traceability_std`
3. Function 定时只查标准表和候选函数
4. success / audit / watermark 都能闭环，回补方式与现有缺陷、产量、玻璃主数据一致

## 3. 源 topic 与目标字段

### 3.1 来源 topic

固定消费下面 5 个 topic：

1. `SGH/8PAA/Chem/Track/C02`
2. `SGH/8PAA/Chem/Track/C03`
3. `SGH/8PAA/Chem/Track/C05`
4. `SGH/8PAA/Chem/Track/C07`
5. `SGH/8PAA/Chem/Track/C09`

### 3.2 源 payload

示例：

```json
{
  "workcenter": "8PAA",
  "station": "8PAA-C02",
  "timestamp": "2026-09-15T14:54:50.000Z",
  "dmc_code": "P1946829-99-E262500012535"
}
```

### 3.3 最终接口业务字段

由 ADX 标准化后生成下面 4 个字段：

```json
{
  "plc_id": "<稳定 hash>",
  "dmc_code": "P1946829-99-E262500012535",
  "data_time": "2026-09-15 14:54:50",
  "report_post": "8PAA-C02"
}
```

生成规则：

1. `plc_id = hash_md5(workcenter|station|source_time|dmc_code)`
2. `dmc_code = payload.dmc_code`
3. `data_time = payload.timestamp` 保持原始时间语义，并格式化为 `yyyy-MM-dd HH:mm:ss`
4. `report_post = station`

## 4. EMQX 侧执行步骤

### 4.1 新建或确认 Event Hub Connector

如果当前 EMQX 已有可复用的 Azure Event Hubs Connector，优先复用现有 namespace 连接。

通常只需要确认：

1. 使用的是 Event Hubs namespace 级连接串
2. 连接串对应的 namespace 有目标 hub 的发送权限
3. 目标 hub 名称已创建

### 4.2 建议的 Event Hub 名称

建议单独新建一个 hub，例如：

`chemical_traceability`

如果客户现场已有更符合命名规范的名称，也可以替换，但后续 ADX Data Connection 和 EMQX 动作都要保持一致。

### 4.3 新建 EMQX Rule

建议 EMQX 仍按之前的统一外层包装发往 Event Hub，保留基础元数据：

```sql
SELECT
  timestamp AS emqx_ts,
  topic,
  clientid AS device_name,
  id AS message_id,
  payload
FROM
  "SGH/8PAA/Chem/Track/+"
WHERE
  topic IN (
    'SGH/8PAA/Chem/Track/C02',
    'SGH/8PAA/Chem/Track/C03',
    'SGH/8PAA/Chem/Track/C05',
    'SGH/8PAA/Chem/Track/C07',
    'SGH/8PAA/Chem/Track/C09'
  )
```

### 4.4 绑定 Event Hub 动作

Rule 动作配置要点：

1. 选择 Azure Event Hubs Connector
2. 目标 hub 选 `chemical_traceability`
3. 输出 body 保持 JSON
4. 发出的 JSON 应包含：
   - `emqx_ts`
   - `topic`
   - `device_name`
   - `message_id`
   - `payload`

建议先用 EMQX 的测试消息验证动作成功，再让现场真实设备发消息。

## 5. Event Hub 侧执行步骤

### 5.1 新建 Hub

在现有 Event Hubs namespace 下新建：

`chemical_traceability`

建议：

1. 保留期不少于 3 天
2. 分区数按现场吞吐量评估；当前量不大时沿用现有低配即可

### 5.2 不需要新建 Function 触发器绑定

这条链路的 Function **不是** Event Hub Trigger。

这里 Event Hub 只负责：

1. 承接 EMQX 原始消息
2. 供 ADX Data Connection 采集

## 6. ADX 侧执行步骤

### 6.1 执行脚本

执行：

`adx/deploy_units/chemical_traceability/43_chemical_traceability_materialize_and_forward.csl`

该脚本会创建或更新下面对象：

1. `raw_chemical_traceability_eh`
2. `raw_chemical_traceability_eh_json`
3. `chemical_traceability_std`
4. `chemical_traceability_forward_success`
5. `chemical_traceability_forward_audit`
6. `fn_chemical_traceability_materialize()`
7. `fn_chemical_traceability_forward_candidates(...)`
8. `chemical_traceability_std` 的 update policy
9. `raw_chemical_traceability_eh` 的 streaming ingestion policy

### 6.2 新建 ADX Data Connection

先确认 ADX cluster 已开启 streaming ingestion。

然后把 Event Hub `chemical_traceability` 接到 ADX 表：

- 目标表：`raw_chemical_traceability_eh`
- Mapping：`raw_chemical_traceability_eh_json`
- Data format：`JSON`
- Ingestion mode：勾选 `Streaming ingestion`

如果现场 Portal 上看不到 `Streaming ingestion` 选项，通常说明是 cluster 级还没开启。

### 6.3 建议的 streaming 配置说明

这次建议**只对 raw 表启用 streaming ingestion**，不扩到整库。

原因：

1. 化学品追溯真正直连 Data Connection 的是 `raw_chemical_traceability_eh`
2. `chemical_traceability_std` 是通过 update policy 从 raw 自动生成
3. 官方文档也说明，如果某张表不是直接被 streaming ingest，而只是经由 update policy 写入，则不需要单独给那张表再定义 streaming ingestion policy

本次脚本里已补：

```kusto
.alter table raw_chemical_traceability_eh policy streamingingestion '{"IsEnabled": true}'
```

注意：

1. 这条命令只管表级策略
2. 要真正生效，仍需先在 ADX cluster 级启用 streaming ingestion
3. Data Connection 创建时也要选择 `Streaming ingestion`

### 6.4 ADX 内部处理逻辑

Data Connection 生效后，链路会自动变成：

1. Event Hub 原始 JSON 落 `raw_chemical_traceability_eh`
2. update policy 调用 `fn_chemical_traceability_materialize()`
3. 自动生成标准表 `chemical_traceability_std`
4. Function 定时查询 `fn_chemical_traceability_forward_candidates(...)`

### 6.5 建议的 ADX 验证 SQL

先看 raw 是否进数：

```kusto
raw_chemical_traceability_eh
| take 10
```

再看标准表是否生成：

```kusto
chemical_traceability_std
| order by watermark_time_utc desc
| take 20
```

再看候选函数是否有数据：

```kusto
fn_chemical_traceability_forward_candidates(
    datetime(1970-01-01),
    120,
    24,
    'chemical_traceability_uat'
)
| take 20
```

## 7. Function 侧执行步骤

### 7.1 部署包

使用部署包：

`dist/sgb_functionapp_forward_all_v20260915_chemical_traceability_v1.zip`

这是基于已验证可用的旧包增量替换脚本后的新包，**不需要重打依赖**。

### 7.2 新增或确认环境变量

最小必需项：

1. `CHEMICAL_TRACEABILITY_SCHEDULE=0 */5 * * * *`
2. `CHEMICAL_TRACEABILITY_INITIAL_WATERMARK=1970-01-01T00:00:00Z`
3. `CHEMICAL_TRACEABILITY_WATERMARK_BLOB_NAME=chemical-traceability-global.json`
4. `CHEMICAL_TRACEABILITY_DRY_RUN=true`
5. `AzureWebJobs.function_chemical_traceability_5m.Disabled=false`

### 7.3 复用的现有环境变量

如果不单独新增化学品追溯专用接口配置，则本链路默认复用下面已有配置：

1. `GLASS_MASTER_API_BASE_URL` / `GLASS_MASTER_BASE_URL`
2. `GLASS_MASTER_API_SYNC_DATA_URL` / `GLASS_MASTER_SYNC_DATA_URL`
3. `INTEGRATION_UAT_APP_ID`
4. `INTEGRATION_UAT_APP_SECRET`
5. `PLATFORM_ADX_CLUSTER_URL`
6. `PLATFORM_ADX_DATABASE`
7. `STATE_STORAGE_ACCOUNT_URL`
8. `WATERMARK_CONTAINER_NAME`

如果希望化学品追溯完全独立配置，也可以单独补：

1. `CHEMICAL_TRACEABILITY_API_BASE_URL`
2. `CHEMICAL_TRACEABILITY_API_SYNC_DATA_URL`
3. `CHEMICAL_TRACEABILITY_API_APP_ID`
4. `CHEMICAL_TRACEABILITY_API_APP_SECRET`
5. `CHEMICAL_TRACEABILITY_ADX_CLUSTER_URL`
6. `CHEMICAL_TRACEABILITY_ADX_DATABASE`

但当前实现并不强制要求单独配置。

### 7.4 Function 名称

本次新增的定时函数名称为：

`function_chemical_traceability_5m`

### 7.5 Function 实际执行逻辑

每 5 分钟执行一次：

1. 读取 watermark
2. 计算 overlap window
3. 查 `fn_chemical_traceability_forward_candidates(...)`
4. 若 `DRY_RUN=true`，只打印日志，不发接口
5. 若 `DRY_RUN=false`，按批次调用接口
6. 每批成功后立即写：
   - `chemical_traceability_forward_success`
   - `chemical_traceability_forward_audit`
7. 推进 watermark
8. 失败时写 `chemical_traceability_forward_audit`

## 8. 最终发给客户接口的报文格式

外层采用 glass master 风格：

```json
{
  "appId": "<app_id>",
  "bizParams": "[{\"plc_id\":\"...\",\"dmc_code\":\"...\",\"data_time\":\"...\",\"report_post\":\"...\"}]",
  "sign": "<md5(app_secret + timestamp + bizParams)>",
  "timestamp": "20260915153045",
  "requestId": "<uuid>"
}
```

说明：

1. `bizParams` 是 JSON 字符串
2. 实际业务内容是数组，不是单对象
3. 当前实现未带 `tenantId`
4. 当前实现未调用 `getNewTime`
5. 时间戳使用本地生成的 `yyyyMMddHHmmss`

## 9. 推荐上线顺序

建议按下面顺序推进：

1. ADX 执行 `adx/deploy_units/chemical_traceability/43_chemical_traceability_materialize_and_forward.csl`
2. Event Hub 新建 `chemical_traceability`
3. ADX 建该 hub 到 `raw_chemical_traceability_eh` 的 Data Connection
4. EMQX 建 Rule + Action，把 5 个 topic 发到该 hub
5. 部署 Function 新 zip
6. Portal 配 `CHEMICAL_TRACEABILITY_*`
7. 先设 `CHEMICAL_TRACEABILITY_DRY_RUN=true`
8. 观察：
   - EMQX 动作成功
   - Event Hub 有 Incoming Messages
   - ADX raw / std 有数据
   - Function 定时日志能查到候选数据
9. 确认无误后把 `CHEMICAL_TRACEABILITY_DRY_RUN=false`
10. 再观察 success / audit 台账与客户接口返回

## 10. 上线后验收点

至少检查下面 6 项：

1. EMQX Rule 命中正常
2. Event Hub `chemical_traceability` 有消息进入
3. `raw_chemical_traceability_eh` 有原始数据
4. `chemical_traceability_std` 已生成 4 个固定字段
5. Function `function_chemical_traceability_5m` 正常触发
6. `chemical_traceability_forward_success` / `chemical_traceability_forward_audit` 正常写入

## 11. 切到生产时要改什么

如果后续要切生产，不需要改代码，主要改环境变量：

1. 将接口 URL 从 UAT 改为生产地址
2. 将 `app_id/app_secret` 切到生产
3. 如需区分账套，可把 `CHEMICAL_TRACEABILITY_FORWARD_TARGET` 从 `chemical_traceability_uat` 改成生产标识

其余 ADX / watermark / success / audit 逻辑可以保持不变。
